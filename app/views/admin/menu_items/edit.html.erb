<h1 class='page-title'>Edit Menu Item</h1>

<%= simple_form_for @item, url: admin_menu_item_category_menu_item_path(@menu_item_category, @item) do |f| %>
  <%= render 'form', f: f %>

  <%= f.submit "Update Menu Item", class: 'btn btn-primary' %>
<% end %>