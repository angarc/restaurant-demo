<h1 class='page-title'>New Menu Item</h1>

<%= simple_form_for @item, url: admin_menu_item_category_menu_items_path(@menu_item_category) do |f| %>
  <%= render 'form', f: f %>

  <%= f.submit "Create New Menu Item", class: 'btn btn-primary' %>
<% end %>